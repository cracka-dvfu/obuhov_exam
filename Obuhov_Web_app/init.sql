-- Этот скрипт выполняется автоматически при первом запуске контейнера MySQL.
-- Создаёт таблицу пользователей.

USE obuhov_db;

CREATE TABLE IF NOT EXISTS users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(100) NOT NULL,
    password VARCHAR(100) NOT NULL
);

-- ВНИМАНИЕ: пароли намеренно хранятся в открытом виде (без хэширования).
-- Это часть уязвимого приложения по заданию — чтобы при SQLi можно было
-- продемонстрировать логин и пароль пользователя Administrator в открытом виде.

-- Один предустановленный пользователь для примера (можно убрать).
-- Administrator и obuhov_so будут добавлены через регистрацию (в т.ч. curl).
